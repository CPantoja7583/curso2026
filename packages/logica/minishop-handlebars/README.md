# MiniShop con Express y Handlebars

## Ejecutar

```bash
npm install
npm start
```

Luego abre:

```text
http://127.0.0.1:3003
```

## Rutas

- `GET /`
- `GET /about`
- `GET /contact`
- `POST /contact`

Las rutas validas responden `405` cuando se usan con un metodo no permitido.
