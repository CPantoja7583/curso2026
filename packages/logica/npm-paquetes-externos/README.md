# Actividad de aprendizaje - paquetes externos de NPM

## Requisitos cumplidos

- Proyecto inicializado con `package.json`
- Dependencias externas: `chalk` y `dayjs`
- Script `npm start`
- Archivo `.gitignore` con `node_modules/`

## Ejecutar

```bash
npm install
npm start
```
